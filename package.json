{
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build --stream",
    "clean": "lerna run clean --stream"
  },
  "devDependencies": {
    "lerna": "^3.13.2"
  }
}
