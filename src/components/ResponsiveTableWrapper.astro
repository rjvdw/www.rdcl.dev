---
export type Props = {
  nowrap?: boolean
}

const { nowrap } = Astro.props
---

<div class:list={['responsive-table-wrapper', { nowrap }]}><slot /></div>

<style lang="scss" is:global>
  .responsive-table-wrapper {
    max-inline-size: 100%;
    margin-block-end: 1rem;
    overflow: auto;

    &.nowrap {
      th,
      td {
        white-space: nowrap;
      }
    }
  }
</style>
